name: Build Axel (Windows Static)

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04  # 使用官方支持的稳定版

    steps:
    - name: Install build dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          git \
          mingw-w64 \
          automake \
          libtool \
          gettext \
          autopoint \
          pkg-config \
          make

    - name: Build and Install Autoconf 2.72
      run: |
        # 下载并编译 Autoconf 2.72
        wget https://ftp.gnu.org/gnu/autoconf/autoconf-2.72.tar.xz
        tar -xf autoconf-2.72.tar.xz
        cd autoconf-2.72
        ./configure --prefix=/usr/local
        make -j$(nproc)
        sudo make install
        # 验证版本
        autoconf --version

    - name: Clone axel source
      run: |
        git clone https://github.com/axel-download-accelerator/axel.git
        cd axel
        git submodule update --init --recursive

    - name: Prepare build environment
      run: |
        echo "CC=x86_64-w64-mingw32-gcc" >> $GITHUB_ENV
        echo "AR=x86_64-w64-mingw32-ar" >> $GITHUB_ENV
        echo "RANLIB=x86_64-w64-mingw32-ranlib" >> $GITHUB_ENV
        echo "STRIP=x86_64-w64-mingw32-strip" >> $GITHUB_ENV
        echo "CFLAGS=-O2 -static" >> $GITHUB_ENV
        echo "LDFLAGS=-static -static-libgcc -static-libstdc++" >> $GITHUB_ENV

    - name: Generate configure script
      run: |
        cd axel
        export AUTOPOINT=true   # 继续跳过 autopoint
        autoreconf -fi

    - name: Configure (Windows static)
      run: |
        cd axel
        ./configure \
          --host=x86_64-w64-mingw32 \
          --disable-nls \
          --disable-shared \
          --enable-static \
          --without-ssl

    - name: Build
      run: |
        cd axel
        make -j$(nproc)

    - name: Strip binary
      run: |
        cd axel
        x86_64-w64-mingw32-strip axel.exe

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: axel-windows-static
        path: axel/axel.exe
